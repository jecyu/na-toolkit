/**
 * Multiplies a value by 2.
 *
 *
 * @param value
 * @returns
 * @anotherNote
 */
function double(value) {
    return value * 2;
}

// Import here Polyfills if needed. Recommended core-js (npm i -D core-js)
var naToolkit = {
    double: double
};

export default naToolkit;
//# sourceMappingURL=na-toolkit.es5.js.map
